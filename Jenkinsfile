pipeline {
    agent any
    stages {
        stage('Stage echo'){
            steps {
                echo 'Start Jenkine File for task CI/CD'
            }
        }
        // stage('Deploy dev environment') {
        //     steps {
        //         sh 'ANSIBLE_HOST_KEY_CHECKING=false ansible-playbook -i playbooks/inventories/dev/hosts playbooks/playbook.yaml'
        //     }
        // }
    }
}

