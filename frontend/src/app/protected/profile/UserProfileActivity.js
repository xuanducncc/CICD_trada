import Row from "antd/lib/row";
import React from "react";
import './style.css';

const UserProfileActivity = ({ user }) => {

  return (
    <Row style={{ display: 'flex', justifyContent: 'center' }}>

    </Row>
  );
};

export default UserProfileActivity;
